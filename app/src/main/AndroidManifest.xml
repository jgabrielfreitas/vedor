<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hackpuc.vedor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activitys.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.CustomNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activitys.MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="YyVUOooeUmORbVk7iP8W6qtpif4NDpLeD4BVBDag" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="td6YRtXiicaU6nTaRt4oOCcvJcNkFtcsrmUWWWeQ" />

        <activity
            android:name=".activitys.StateActivity"
            android:configChanges="orientation|keyboardHidden|screenSize" />
        <activity
            android:name=".activitys.CandidateActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/candidate" />

        <meta-data
            android:name="FAVORITES_DB_NAME"
            android:value="Favorites.db" />
        <meta-data
            android:name="FAVORITES_DB_VERSION"
            android:value="1" />

        <activity
            android:name=".activitys.DetailsActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/details" />
        <activity
            android:name=".activitys.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activitys.CreateAccountActivity"
            android:label="Criar conta"
            android:theme="@style/AppTheme" />
        <activity android:name=".activitys.CreatePostActivity" />
        <activity android:name=".activitys.FeedActivity"></activity>
    </application>

</manifest>
